spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gunstoreDB?createDatabaseIfNotExist=true
    username: root
    password: akeed
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
              
